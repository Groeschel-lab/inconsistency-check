# Azure Functions (Python) app dependencies - vendored into the release zip.
# Pinned so a given tag always builds the same package. Resolved 2026-09-22 for cp311/manylinux.
# Regenerate: pip install -r requirements.txt --dry-run --report r.json --target tmp \
#   --platform manylinux_2_28_x86_64 --implementation cp --python-version 3.11 --only-binary=:all:

# Direct dependencies
azure-functions==1.25.0
azure-identity==1.25.3
fastapi==0.141.1
pydantic==2.13.5
requests==2.34.2

# Transitive dependencies
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
azure-core==1.41.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
cryptography==50.0.1
idna==3.19
MarkupSafe==3.0.3
msal==1.39.0
msal-extensions==1.3.1
pycparser==3.0
pydantic_core==2.46.5
PyJWT==2.14.0
starlette==1.6.0
typing_extensions==4.16.0
typing-inspection==0.4.4
urllib3==2.7.0
Werkzeug==3.1.8
